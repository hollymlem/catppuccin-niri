---
whiskers:
  version: ^2.5.1
  matrix:
    - flavor
    - accent
  filename: themes/{{ flavor.identifier }}/catppuccin-{{ flavor.identifier }}-{{ accent }}.kdl
  hex_format: "#{{r}}{{g}}{{b}}"
---
layout {
    # Theme: Catppuccin {{ flavor.name }} - {{ accent }}
    background-color "{{ base.hex }}"

    focus-ring {
        active-color "{{ flavor.colors[accent].hex }}"
        inactive-color "{{ surface0.hex }}"
        urgent-color "{{ red.hex }}"
    }

    border {
        active-color "{{ flavor.colors[accent].hex }}"
        inactive-color "{{ surface0.hex }}"
        urgent-color "{{ red.hex }}"
    }

    shadow {
        color "{{ crust | mod(opacity=0.3) | get(key="hex") }}"
        inactive-color "{{ base | mod(opacity=0.3) | get(key="hex") }}"
    }

    tab-indicator {
        active-color "{{ flavor.colors[accent].hex }}"
        inactive-color "{{ surface0.hex }}"
        urgent-color "{{ red.hex }}"
    }

    insert-hint {
        color "{{ peach.hex }}"
    }
}

recent-windows {
    highlight {
        active-color "{{ flavor.colors[accent].hex }}"
        urgent-color "{{ red.hex }}"
    }
}

overview {
    backdrop-color "{{ mantle.hex }}"
    workspace-shadow {
        color "{{ crust | mod(opacity=0.3) | get(key="hex") }}"
        inactive-color "{{ base | mod(opacity=0.3) | get(key="hex") }}"
    }
}
